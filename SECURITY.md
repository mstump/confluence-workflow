# SECURITY.md

Generated by: gsd-secure-phase auditor
Phase: 04 — cli-command-wiring-and-integration
ASVS Level: 1
Audit date: 2026-04-13

---

## Threat Verification Results

All 7 registered threats verified. 7/7 CLOSED.

| Threat ID | Category               | Disposition | Status | Evidence                                                                                                                                                                                                                                                                                                |
| --------- | ---------------------- | ----------- | ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-04-01   | Tampering              | accept      | CLOSED | Accepted risk — single-user filesystem write via `create_dir_all` + `output_dir.join()` at `src/lib.rs:212,215,221`; user controls their own output directory; documented in `04-01-PLAN.md` threat register                                                                                            |
| T-04-02   | Information Disclosure | mitigate    | CLOSED | `src/llm/mod.rs` tracing spans log only `status`, `attempt`, `delay_ms`, `decision` — `api_key` field is never passed to any `tracing::` macro; `request_with_retry` at line 90 adds the key only to the `x-api-key` HTTP header, not to any span                                                       |
| T-04-03   | Information Disclosure | mitigate    | CLOSED | `src/error.rs:86-88` — `ConfigError::Missing` Display emits `"Missing required configuration: {name}"` using the env var name string (`&'static str`), never the value; `ConfluenceError::Unauthorized` at line 120-123 shows no credentials; all other Display impls confirmed credential-value-free   |
| T-04-04   | Denial of Service      | accept      | CLOSED | Accepted risk — single-user CLI; user controls input file size; no memory limits needed beyond OS defaults; documented in `04-01-PLAN.md` threat register                                                                                                                                               |
| T-04-05   | Information Disclosure | mitigate    | CLOSED | `src/main.rs:13-16` — `init_tracing()` calls `fmt().with_env_filter(...).with_writer(std::io::stderr).init()`; all tracing output is routed exclusively to stderr                                                                                                                                       |
| T-04-06   | Information Disclosure | mitigate    | CLOSED | `src/lib.rs:71-76` — `error_to_json()` serializes `error.to_string()` which delegates to thiserror Display impls; none of the `AppError` / `ConfigError` / `ConfluenceError` Display impls include credential values, only field names and descriptive strings                                          |
| T-04-07   | Information Disclosure | mitigate    | CLOSED | `src/lib.rs:22-35` — `CommandResult` enum contains only `page_url: String`, `comments_kept: usize`, `comments_dropped: usize`, `output_dir: String`, `files: Vec<String>`; no credential fields are present; `result_to_json()` at lines 38-66 serializes only those safe fields                        |

---

## Accepted Risks Log

| Threat ID | Category          | Risk Summary                              | Rationale                                                                                                                                                |
| --------- | ----------------- | ----------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-04-01   | Tampering         | User-controlled output_dir path traversal | User runs the CLI against their own filesystem with a path they provide; no privilege escalation possible; scope is limited to user-writable directories |
| T-04-04   | Denial of Service | No memory limits on markdown file reads   | Single-user CLI tool; adversarial large-file input requires local filesystem access which implies local access already; OS memory limits apply           |

---

## Unregistered Threat Flags

None. Neither `04-01-SUMMARY.md` nor `04-02-SUMMARY.md` contained a `## Threat Flags` section. No new attack surface was flagged during implementation.

---

## Files Audited

- `src/lib.rs`
- `src/main.rs`
- `src/cli.rs`
- `src/error.rs`
- `src/llm/mod.rs`
- `.planning/phases/04-cli-command-wiring-and-integration/04-01-PLAN.md`
- `.planning/phases/04-cli-command-wiring-and-integration/04-02-PLAN.md`
- `.planning/phases/04-cli-command-wiring-and-integration/04-01-SUMMARY.md`
- `.planning/phases/04-cli-command-wiring-and-integration/04-02-SUMMARY.md`
