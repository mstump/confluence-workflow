---
description: "Validate type checking with mypy after modifying Python files."
globs: ["**/*.py"]
alwaysApply: true
---

# Validate Type Checking

After modifying Python code, validate it with `mypy` to catch type errors.

**Command:**

```bash
uv run mypy .
```

**Configuration:**
`mypy` is configured in `pyproject.toml`.
