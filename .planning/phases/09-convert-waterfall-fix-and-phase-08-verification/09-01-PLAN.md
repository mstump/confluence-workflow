---
phase: 09-convert-waterfall-fix-and-phase-08-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/08-diagramconfig-waterfall-and-nyquist-compliance/08-VERIFICATION.md
autonomous: true
requirements:
  - SCAF-03
user_setup: []
tags:
  - verification
  - phase-08
  - goal-backward
  - documentation

must_haves:
  truths:
    - "Phase 08 VERIFICATION.md file exists at .planning/phases/08-diagramconfig-waterfall-and-nyquist-compliance/08-VERIFICATION.md"
    - "The file has valid YAML frontmatter with phase, verified, status, score, overrides_applied keys"
    - "status is 'passed' because Phase 08 has no human-verification items (08-VALIDATION.md line 61: 'All phase behaviors have automated verification')"
    - "Every must-have truth from 08-01-PLAN.md (5 truths) and 08-02-PLAN.md (4 truths) is listed in a Goal Achievement table with VERIFIED status and file:line evidence"
    - "Every SCAF-03 requirement row in Requirements Coverage cites the specific file+line+code-excerpt supporting the claim"
    - "The file follows the multi-section structure established by 06-VERIFICATION.md and 07-VERIFICATION.md (Observable Truths, Required Artifacts, Key Link Verification, Requirements Coverage, Anti-Patterns, Human Verification, Gaps Summary)"
  artifacts:
    - path: ".planning/phases/08-diagramconfig-waterfall-and-nyquist-compliance/08-VERIFICATION.md"
      provides: "Goal-backward verification evidence for Phase 08's 9 must-have truths (5 from 08-01, 4 from 08-02) against the current codebase at Phase 08 close"
      contains: "nyquist_compliant"
  key_links:
    - from: "08-VERIFICATION.md Observable Truths table"
      to: "src/cli.rs, src/config.rs, src/lib.rs, tests/cli_integration.rs"
      via: "File path + line number + code excerpt in each Evidence cell"
      pattern: "src/(cli|config|lib)\\.rs"
    - from: "08-VERIFICATION.md Requirements Coverage row"
      to: "REQUIREMENTS.md SCAF-03 definition"
      via: "Requirement ID cross-reference"
      pattern: "SCAF-03"
---

<objective>
Author a goal-backward VERIFICATION.md for Phase 08 (DiagramConfig Waterfall and Nyquist Compliance) by grep-ing the current codebase for structural evidence of each Phase 08 must-have truth. This is a pure authoring task producing one new markdown file; no source code is touched.

Purpose: The v1.0 milestone audit identified Phase 08 as the only completed phase without a VERIFICATION.md. This plan closes that audit gap by producing the artifact using the goal-backward methodology and the format established by 06-VERIFICATION.md and 07-VERIFICATION.md. Ordering rationale: this plan runs in Wave 1 BEFORE Plan 09-02's code refactor so the evidence cited in VERIFICATION.md reflects Phase 08's state at close (2026-04-16 / commit c4997ef), not the post-refactor state that 09-02 will produce. See 09-RESEARCH.md Pitfall 5.

Output: `.planning/phases/08-diagramconfig-waterfall-and-nyquist-compliance/08-VERIFICATION.md` (one new file, ~150–200 lines, following the 07-VERIFICATION.md template).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-convert-waterfall-fix-and-phase-08-verification/09-CONTEXT.md
@.planning/phases/09-convert-waterfall-fix-and-phase-08-verification/09-RESEARCH.md
@.planning/phases/09-convert-waterfall-fix-and-phase-08-verification/09-PATTERNS.md

<!-- Phase 08 source artifacts — the must-haves and evidence to audit -->
@.planning/phases/08-diagramconfig-waterfall-and-nyquist-compliance/08-01-PLAN.md
@.planning/phases/08-diagramconfig-waterfall-and-nyquist-compliance/08-02-PLAN.md
@.planning/phases/08-diagramconfig-waterfall-and-nyquist-compliance/08-01-SUMMARY.md
@.planning/phases/08-diagramconfig-waterfall-and-nyquist-compliance/08-02-SUMMARY.md
@.planning/phases/08-diagramconfig-waterfall-and-nyquist-compliance/08-VALIDATION.md

<!-- Reference format — copy structure verbatim -->
@.planning/phases/06-credential-waterfall-fix/06-VERIFICATION.md
@.planning/phases/07-test-scaffold-completion/07-VERIFICATION.md

<!-- Codebase files that evidence must cite -->
@src/cli.rs
@src/config.rs
@src/lib.rs
@tests/cli_integration.rs

<interfaces>
<!-- The 9 must-have truths from Phase 08 that VERIFICATION.md must evidence -->

From 08-01-PLAN.md must_haves.truths (5 items):

1. "`--plantuml-path /custom/path convert doc.md ./out` uses /custom/path for PlantUML rendering, not the default"
2. "`--mermaid-path /custom/mmdc upload doc.md <url>` uses /custom/mmdc for Mermaid rendering"
3. "DiagramConfig is resolved inside Config::load() for update/upload arms, not constructed independently via from_env()"
4. "Convert arm respects --plantuml-path and --mermaid-path without requiring Confluence credentials"
5. "Default behavior unchanged: omitting flags falls back to env var, then to 'plantuml'/'mmdc' defaults"

From 08-02-PLAN.md must_haves.truths (4 items):

1. "Phase 01 VALIDATION.md has YAML frontmatter with nyquist_compliant: true and wave_0_complete: true"
2. "Phase 02 VALIDATION.md has nyquist_compliant: true and wave_0_complete: true in frontmatter"
3. "Phase 03 VALIDATION.md has nyquist_compliant: true and wave_0_complete: true in frontmatter"
4. "All three VALIDATION.md files have an audited: date field"

Total: 9 truths across two plans.

From 08-VALIDATION.md Per-Task Verification Map (6 tests, all ✅ green as of 2026-04-15):

- test_plantuml_path_cli_override (src/config.rs:539)
- test_mermaid_path_cli_override (src/config.rs:564)
- test_diagram_config_defaults_when_no_override (src/config.rs:589)
- test_convert_with_diagram_path_flags (tests/cli_integration.rs:348)
- cargo test --lib config::tests (Wave 0 suite)
- grep for nyquist_compliant: true in 01/02/03-VALIDATION.md

Evidence file locations (verified in 09-PATTERNS.md):

- src/cli.rs:33-39 — plantuml_path and mermaid_path fields with #[arg(long, env = "...")]
- src/config.rs:46-54 — CliOverrides struct with plantuml_path/mermaid_path fields
- src/config.rs:55-66 — Config struct with diagram_config: DiagramConfig field
- src/config.rs:137-160 — waterfall resolution of plantuml_path/mermaid_path + DiagramConfig construction
- src/config.rs:539-615 — 3 new unit tests (test_plantuml_path_cli_override, test_mermaid_path_cli_override, test_diagram_config_defaults_when_no_override)
- src/lib.rs:84-91 — Update arm CliOverrides with plantuml_path/mermaid_path
- src/lib.rs:103 — Update arm uses MarkdownConverter::new(config.diagram_config.clone())
- src/lib.rs:164-171 — Upload arm CliOverrides with plantuml_path/mermaid_path
- src/lib.rs:176 — Upload arm uses MarkdownConverter::new(config.diagram_config.clone())
- src/lib.rs:207-227 — Convert arm with inline DiagramConfig (reads cli.plantuml_path/cli.mermaid_path)
- tests/cli_integration.rs:348-406 — test_convert_with_diagram_path_flags
- .planning/phases/01-.../01-VALIDATION.md — nyquist_compliant: true, wave_0_complete: true
- .planning/phases/02-.../02-VALIDATION.md — nyquist_compliant: true, wave_0_complete: true
- .planning/phases/03-.../03-VALIDATION.md — nyquist_compliant: true, wave_0_complete: true
</interfaces>

</context>

<tasks>

<task type="auto">
  <name>Task 1: Gather evidence from codebase and Phase 08 planning artifacts</name>
  <files></files>
  <read_first>
    - .planning/phases/08-diagramconfig-waterfall-and-nyquist-compliance/08-01-PLAN.md (must_haves section lines 16-41 for 5 Plan 01 truths)
    - .planning/phases/08-diagramconfig-waterfall-and-nyquist-compliance/08-02-PLAN.md (must_haves section lines 15-31 for 4 Plan 02 truths)
    - .planning/phases/08-diagramconfig-waterfall-and-nyquist-compliance/08-01-SUMMARY.md (implementation evidence)
    - .planning/phases/08-diagramconfig-waterfall-and-nyquist-compliance/08-02-SUMMARY.md (frontmatter-edit evidence)
    - .planning/phases/08-diagramconfig-waterfall-and-nyquist-compliance/08-VALIDATION.md (6-test per-task map, all green)
    - .planning/phases/06-credential-waterfall-fix/06-VERIFICATION.md (reference: single-plan, no overrides, passed)
    - .planning/phases/07-test-scaffold-completion/07-VERIFICATION.md (reference: multi-plan, overrides_applied: 0 with override example, passed)
    - src/cli.rs (lines 33-39: plantuml_path/mermaid_path fields)
    - src/config.rs (lines 45-54: CliOverrides; lines 55-66: Config struct with diagram_config; lines 137-160: waterfall for diagram paths; lines 539-615: 3 new tests)
    - src/lib.rs (lines 84-91, 103: Update arm wiring; lines 164-171, 176: Upload arm wiring; lines 207-227: Convert arm)
    - tests/cli_integration.rs (lines 348-406: test_convert_with_diagram_path_flags)
    - .planning/phases/01-project-scaffolding-and-confluence-api-client/01-VALIDATION.md (lines 1-8: frontmatter)
    - .planning/phases/02-markdown-to-confluence-storage-format-converter/02-VALIDATION.md (lines 1-8: frontmatter)
    - .planning/phases/03-llm-client-and-comment-preserving-merge/03-VALIDATION.md (lines 1-8: frontmatter)
  </read_first>
  <action>
This task produces NO file output — it is an evidence-gathering pass whose output is the mental model used by Task 2. Execute the following 9 grep/read checks against the current working tree and record the findings (file path + line numbers + matching text excerpt) for each of Phase 08's 9 must-have truths.

**Note on ordering:** This plan (09-01) runs in Wave 1, BEFORE Plan 09-02's code refactor. All line numbers below refer to the CURRENT state of `src/cli.rs`, `src/config.rs`, `src/lib.rs`, `tests/cli_integration.rs` — i.e., Phase 08's state at close. The Phase 08 last-commit SHA is `c4997ef` (per 08-02-SUMMARY.md).

**For Plan 08-01 truths (5 items) — evidence checks:**

1. Truth #1: `--plantuml-path /custom/path convert doc.md ./out` uses /custom/path for PlantUML rendering
   - Evidence command: `grep -n "plantuml_path" src/cli.rs src/config.rs src/lib.rs`
   - Expected: src/cli.rs:34-35 has `#[arg(long, env = "PLANTUML_PATH")] pub plantuml_path: Option<String>`; src/config.rs:137-141 has `Self::resolve_optional(overrides.plantuml_path.as_deref(), "PLANTUML_PATH", home).unwrap_or_else(|| "plantuml".to_string())`; src/lib.rs:89 has `plantuml_path: cli.plantuml_path.clone()` in Update arm CliOverrides; tests/cli_integration.rs:353-354 exercises `--plantuml-path /fake/plantuml` flag.
   - Also: unit test proves this at src/config.rs:539 `test_plantuml_path_cli_override` asserts `config.diagram_config.plantuml_path == "/custom/plantuml"`.

2. Truth #2: `--mermaid-path /custom/mmdc upload doc.md <url>` uses /custom/mmdc for Mermaid rendering
   - Evidence command: `grep -n "mermaid_path" src/cli.rs src/config.rs src/lib.rs`
   - Expected: src/cli.rs:38-39 has `#[arg(long, env = "MERMAID_PATH")] pub mermaid_path: Option<String>`; src/config.rs:143-147 has `Self::resolve_optional(overrides.mermaid_path.as_deref(), "MERMAID_PATH", home).unwrap_or_else(|| "mmdc".to_string())`; src/lib.rs:170 has `mermaid_path: cli.mermaid_path.clone()` in Upload arm.
   - Also: unit test proves this at src/config.rs:564 `test_mermaid_path_cli_override` asserts `config.diagram_config.mermaid_path == "/custom/mmdc"`.

3. Truth #3: DiagramConfig is resolved inside Config::load() for update/upload arms, not constructed independently via from_env()
   - Evidence command: `grep -n "diagram_config\|MarkdownConverter::" src/lib.rs`
   - Expected: src/lib.rs:103 `let converter = MarkdownConverter::new(config.diagram_config.clone());` (Update arm, NOT `::default()`); src/lib.rs:176 same pattern (Upload arm); src/config.rs:155-160 builds `DiagramConfig { plantuml_path, mermaid_path, mermaid_puppeteer_config, timeout_secs }` inside `load_with_home` and returns it as `Config.diagram_config`.
   - Anti-pattern check: `grep -n "MarkdownConverter::default()" src/lib.rs` — MUST return zero matches in update/upload arms (convert arm is allowed to use inline construction per 08-01-PLAN).
   - Anti-pattern check: `grep -n "DiagramConfig::from_env()" src/lib.rs` — MUST return zero matches in any arm.

4. Truth #4: Convert arm respects --plantuml-path and --mermaid-path without requiring Confluence credentials
   - Evidence command: inspect src/lib.rs:207-227 (Convert arm body)
   - Expected: src/lib.rs:211 comment `// No Config::load() needed -- convert does not require Confluence credentials`; src/lib.rs:214-226 constructs `DiagramConfig` inline using `cli.plantuml_path.or_else(|| std::env::var("PLANTUML_PATH").ok()).unwrap_or_else(|| "plantuml".to_string())` and same pattern for mermaid_path; src/lib.rs:227 `let converter = MarkdownConverter::new(diagram_config);`.
   - Integration test proves this: tests/cli_integration.rs:348-406 `test_convert_with_diagram_path_flags` uses `env_remove("CONFLUENCE_URL")` + `env_remove("CONFLUENCE_USERNAME")` + `env_remove("CONFLUENCE_API_TOKEN")` at lines 360-362 and asserts `output.status.success()`.

5. Truth #5: Default behavior unchanged: omitting flags falls back to env var, then to 'plantuml'/'mmdc' defaults
   - Evidence command: unit test at src/config.rs:589 `test_diagram_config_defaults_when_no_override`
   - Expected: lines 596-599 remove PLANTUML_PATH/MERMAID_PATH env vars; lines 601-602 invoke `Config::load_with_home`; lines 613-614 assert `config.diagram_config.plantuml_path == "plantuml"` and `config.diagram_config.mermaid_path == "mmdc"`.
   - Also: waterfall resolve_optional at src/config.rs:206-227 implements CLI → env var → ~/.claude/ → default fallback.

**For Plan 08-02 truths (4 items) — evidence checks:**

1. Truth #6: Phase 01 VALIDATION.md has YAML frontmatter with nyquist_compliant: true and wave_0_complete: true
   - Evidence command: `head -10 .planning/phases/01-project-scaffolding-and-confluence-api-client/01-VALIDATION.md`
   - Expected: lines 1-8 frontmatter contains `status: verified`, `nyquist_compliant: true`, `wave_0_complete: true`, `audited: 2026-04-14` (or similar).

2. Truth #7: Phase 02 VALIDATION.md has nyquist_compliant: true and wave_0_complete: true
   - Evidence command: `head -10 .planning/phases/02-markdown-to-confluence-storage-format-converter/02-VALIDATION.md`
   - Expected: lines 1-8 frontmatter contains `nyquist_compliant: true` (was `false` pre-Phase-08), `wave_0_complete: true` (was `false` pre-Phase-08), `status: verified` (was `draft`).

3. Truth #8: Phase 03 VALIDATION.md has nyquist_compliant: true and wave_0_complete: true
   - Evidence command: `head -10 .planning/phases/03-llm-client-and-comment-preserving-merge/03-VALIDATION.md`
   - Expected: lines 1-8 frontmatter contains `nyquist_compliant: true`, `wave_0_complete: true`, `status: verified`.

4. Truth #9: All three VALIDATION.md files have an audited: date field
   - Evidence command: `grep -n "^audited:" .planning/phases/0[123]-*/0[123]-VALIDATION.md`
   - Expected: 3 matches, each with a date value (e.g., `audited: 2026-04-14`).

**Spot-check: run `cargo test --lib config::tests -- --test-threads=1`** to confirm the 3 Phase-08-added tests (test_plantuml_path_cli_override, test_mermaid_path_cli_override, test_diagram_config_defaults_when_no_override) still pass in the current tree. If any fail, record the failure and set VERIFICATION.md `status: gaps_found` in Task 2 rather than `passed`.

**Spot-check: run `cargo test --test cli_integration test_convert_with_diagram_path_flags`** to confirm the integration test still passes. Record pass/fail.

**No file is written in this task.** The output is a short structured note (in the agent's working memory / summary) listing each of the 9 truths with the file:line+excerpt evidence gathered. This note drives Task 2's VERIFICATION.md authoring.
  </action>
  <verify>
    <automated>cd /Users/matthewstump/src/confluence-workflow && cargo test --lib config::tests::test_plantuml_path_cli_override -- --exact && cargo test --lib config::tests::test_mermaid_path_cli_override -- --exact && cargo test --lib config::tests::test_diagram_config_defaults_when_no_override -- --exact && cargo test --test cli_integration test_convert_with_diagram_path_flags</automated>
  </verify>
  <acceptance_criteria>
    - Grep `plantuml_path` returns matches in src/cli.rs AND src/config.rs AND src/lib.rs (3 files minimum)
    - Grep `mermaid_path` returns matches in src/cli.rs AND src/config.rs AND src/lib.rs (3 files minimum)
    - Grep `diagram_config` returns matches in src/config.rs AND src/lib.rs (2 files minimum)
    - Grep `MarkdownConverter::default()` in src/lib.rs returns zero matches
    - Grep `DiagramConfig::from_env()` in src/lib.rs returns zero matches
    - Grep `nyquist_compliant: true` returns 3 matches across .planning/phases/0[123]-*/0[123]-VALIDATION.md
    - Grep `wave_0_complete: true` returns 3 matches across .planning/phases/0[123]-*/0[123]-VALIDATION.md
    - Grep `audited:` returns 3 matches across .planning/phases/0[123]-*/0[123]-VALIDATION.md
    - `cargo test --lib config::tests::test_plantuml_path_cli_override -- --exact` exits 0
    - `cargo test --lib config::tests::test_mermaid_path_cli_override -- --exact` exits 0
    - `cargo test --lib config::tests::test_diagram_config_defaults_when_no_override -- --exact` exits 0
    - `cargo test --test cli_integration test_convert_with_diagram_path_flags` exits 0
  </acceptance_criteria>
  <done>All 9 Phase-08 truths have concrete file:line evidence gathered and recorded; all 4 automated tests confirm the behavior is live in the current tree.</done>
</task>

<task type="auto">
  <name>Task 2: Author 08-VERIFICATION.md using the 07-VERIFICATION.md template</name>
  <files>.planning/phases/08-diagramconfig-waterfall-and-nyquist-compliance/08-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/07-test-scaffold-completion/07-VERIFICATION.md (primary template — multi-plan phase with overrides block structure)
    - .planning/phases/06-credential-waterfall-fix/06-VERIFICATION.md (secondary template — single-plan phase, simpler)
    - .planning/phases/08-diagramconfig-waterfall-and-nyquist-compliance/08-VALIDATION.md (the 6-test per-task map to cite in Behavioral Spot-Checks)
    - .planning/phases/08-diagramconfig-waterfall-and-nyquist-compliance/08-01-PLAN.md (must_haves.artifacts and must_haves.key_links for the Required Artifacts and Key Link tables)
    - .planning/phases/08-diagramconfig-waterfall-and-nyquist-compliance/08-02-PLAN.md (must_haves.artifacts for Plan 02 artifacts)
  </read_first>
  <action>
Write the file `.planning/phases/08-diagramconfig-waterfall-and-nyquist-compliance/08-VERIFICATION.md` with the following structure. Copy the **exact** section order from 07-VERIFICATION.md; swap content for Phase 08.

**Frontmatter (exact values):**

```yaml
---
phase: 08-diagramconfig-waterfall-and-nyquist-compliance
verified: 2026-04-20T00:00:00Z
status: passed
score: 9/9 must-haves verified
overrides_applied: 0
---
```

If Task 1 uncovered a test failure or missing evidence, change `status` to `gaps_found` and set `score` to the actual verified count (e.g., `8/9 must-haves verified`). If an override is applied (e.g., a must-have wording is stricter than what was implemented but goal is still achieved), add the `overrides:` list block from 07-VERIFICATION.md lines 7-11.

**Body structure (follow this exact section order):**

1. **H1 title**: `# Phase 8: DiagramConfig Waterfall and Nyquist Compliance Verification Report`

2. **Preamble block**:
   - `**Phase Goal:** DiagramConfig respects the same CLI > env > config waterfall as credentials; Phases 01-03 achieve Nyquist compliance with proper VALIDATION.md frontmatter` (copy verbatim from ROADMAP.md Phase 8 Goal)
   - `**Verified:** 2026-04-20`
   - `**Status:** passed`
   - `**Re-verification:** No — initial verification`

3. **## Goal Achievement** → **### Observable Truths** table with columns `#`, `Truth`, `Status`, `Evidence`. Include all 9 truths from Task 1 findings. Example row format (for Truth #1):

   | 1 | `--plantuml-path /custom/path convert doc.md ./out` uses /custom/path for PlantUML rendering | VERIFIED | `src/cli.rs:33-35`: `#[arg(long, env = "PLANTUML_PATH")] pub plantuml_path: Option<String>`; `src/config.rs:137-141`: `resolve_optional(overrides.plantuml_path.as_deref(), "PLANTUML_PATH", home).unwrap_or_else(\|\| "plantuml".to_string())`; `src/lib.rs:89`: `plantuml_path: cli.plantuml_path.clone()` in Update arm; unit test `test_plantuml_path_cli_override` (src/config.rs:539) asserts `config.diagram_config.plantuml_path == "/custom/plantuml"`; integration test `test_convert_with_diagram_path_flags` (tests/cli_integration.rs:348-406) exercises `--plantuml-path /fake/plantuml`. |

   Populate rows 2-9 identically using Task 1 findings. End with `**Score:** 9/9 truths verified`.

4. **### Deferred Items** — `None.` (per 09-CONTEXT.md D-07: no deferred items for Phase 08)

5. **### Required Artifacts** — 4-column table (`Artifact`, `Expected`, `Status`, `Details`). Include one row per file listed in 08-01-PLAN.md `must_haves.artifacts` (src/cli.rs, src/config.rs, src/lib.rs) and 08-02-PLAN.md `must_haves.artifacts` (01-VALIDATION.md, 02-VALIDATION.md, 03-VALIDATION.md). Each `Details` cell must cite line numbers.

   Expected row for src/cli.rs:
   | `src/cli.rs` | `plantuml_path`/`mermaid_path` Option<String> fields on Cli | VERIFIED | Lines 33-35 declare `plantuml_path` with `#[arg(long, env = "PLANTUML_PATH")]`; lines 37-39 declare `mermaid_path` with `#[arg(long, env = "MERMAID_PATH")]` |

   Expected row for src/config.rs:
   | `src/config.rs` | CliOverrides with plantuml_path/mermaid_path; Config with diagram_config; waterfall resolution | VERIFIED | CliOverrides at lines 45-54 contains `pub plantuml_path: Option<String>` (line 52) and `pub mermaid_path: Option<String>` (line 53); Config struct at lines 57-66 contains `pub diagram_config: DiagramConfig` (line 65); waterfall resolution at lines 137-160 builds DiagramConfig from resolve_optional calls |

   Expected row for src/lib.rs:
   | `src/lib.rs` | `MarkdownConverter::new(config.diagram_config)` in update/upload; inline DiagramConfig in convert | VERIFIED | Update arm line 103: `MarkdownConverter::new(config.diagram_config.clone())`; Upload arm line 176: same; Convert arm lines 214-227: inline DiagramConfig construction; zero occurrences of `MarkdownConverter::default()` |

   Expected rows for 01/02/03-VALIDATION.md:
   | `.planning/phases/01-.../01-VALIDATION.md` | Frontmatter with nyquist_compliant: true, wave_0_complete: true, audited | VERIFIED | Lines 1-8: `phase: 1`, `status: verified`, `nyquist_compliant: true`, `wave_0_complete: true`, `audited: 2026-04-14` (record actual line numbers and exact key/value pairs observed in Task 1) |
   | `.planning/phases/02-.../02-VALIDATION.md` | same as Phase 01 | VERIFIED | Lines 1-8: `nyquist_compliant: true`, `wave_0_complete: true`, `status: verified`, `audited: 2026-04-14` |
   | `.planning/phases/03-.../03-VALIDATION.md` | same | VERIFIED | Lines 1-8: `nyquist_compliant: true`, `wave_0_complete: true`, `status: verified`, `audited: 2026-04-14` |

6. **### Key Link Verification** — 5-column table (`From`, `To`, `Via`, `Status`, `Details`). Include rows from 08-01-PLAN.md `must_haves.key_links`:

   | `src/cli.rs` | `src/lib.rs` | `cli.plantuml_path` / `cli.mermaid_path` forwarded to CliOverrides | WIRED | Update arm line 89-90: `plantuml_path: cli.plantuml_path.clone(), mermaid_path: cli.mermaid_path.clone()`; Upload arm line 169-170: same pattern |
   | `src/config.rs` | `src/lib.rs` | `config.diagram_config` consumed by `MarkdownConverter::new()` | WIRED | Update arm line 103: `MarkdownConverter::new(config.diagram_config.clone())`; Upload arm line 176: same |

7. **### Data-Flow Trace (Level 4)** — `Not applicable. Phase 08 wires config values; no component renders dynamic user-facing data.` (copy language pattern from 07-VERIFICATION.md line 59)

8. **### Behavioral Spot-Checks** — 4-column table (`Behavior`, `Command`, `Result`, `Status`). Pull entries from 08-VALIDATION.md Per-Task Verification Map (lines 42-47):

   | plantuml_path CLI override wired through Config | `cargo test --lib config::tests::test_plantuml_path_cli_override -- --exact` | 1 test passed | PASS |
   | mermaid_path CLI override wired through Config | `cargo test --lib config::tests::test_mermaid_path_cli_override -- --exact` | 1 test passed | PASS |
   | diagram defaults used when no override | `cargo test --lib config::tests::test_diagram_config_defaults_when_no_override -- --exact` | 1 test passed | PASS |
   | Convert arm accepts --plantuml-path and --mermaid-path flags | `cargo test --test cli_integration test_convert_with_diagram_path_flags` | 1 test passed | PASS |
   | Phase 01/02/03 VALIDATION.md have compliant frontmatter | `grep -c "nyquist_compliant: true" .planning/phases/0[123]-*/0[123]-VALIDATION.md` | 3 matches | PASS |
   | No MarkdownConverter::default() remains in lib.rs | `grep -c "MarkdownConverter::default()" src/lib.rs` | 0 | PASS |

9. **### Requirements Coverage** — 5-column table (`Requirement`, `Source Plan`, `Description`, `Status`, `Evidence`). One row for SCAF-03:

   | SCAF-03 | 08-01-PLAN.md, 08-02-PLAN.md | Credentials/config loaded via waterfall (diagram path tier) | SATISFIED | 08-01 implemented three-tier waterfall for plantuml_path/mermaid_path (CLI → env → default) in `Config::load_with_home` lines 137-147; 08-02 audited Phases 01/02/03 VALIDATION.md to nyquist_compliant: true. Note: SCAF-03 has an outstanding integration gap in the convert arm (uses inline DiagramConfig, not Config::load); this is tracked for Phase 9 per REQUIREMENTS.md traceability row "SCAF-03 | Phase 6 / Phase 9 (gap closure)". |

10. **### Anti-Patterns Found** — 5-column table (`File`, `Line`, `Pattern`, `Severity`, `Impact`). Expected outcome: `(none) | — | — | — | —` — no anti-patterns because:
    - No `MarkdownConverter::default()` remains in lib.rs (Task 1 grep)
    - No `DiagramConfig::from_env()` call remains in lib.rs production code (Task 1 grep)
    - Note: src/config.rs:22-37 still defines `DiagramConfig::from_env()` and src/config.rs:39-43 still defines `impl Default for DiagramConfig` — these remain because test-only code in `src/converter/` references them. This is documented tech debt tracked in Phase 10 (REQUIREMENTS traceability). Include a single NOTE row flagging this tech debt as `Info`-severity, not a blocker.

    Sample NOTE row:
    | `src/config.rs` | 22-43 | `DiagramConfig::from_env()` and `impl Default for DiagramConfig` remain public | Info | Not a regression — these APIs are test-fixture helpers with no production callers post-Phase 08 (all three command arms in lib.rs use explicit DiagramConfig construction). Tracked as tech debt for Phase 10 removal per REQUIREMENTS.md traceability. Not a blocker for Phase 08 verification. |

11. **### Human Verification Required** — `None.` (per 08-VALIDATION.md line 61 and 09-RESEARCH.md assumption A5; all Phase 08 behaviors have automated verification)

12. **### Gaps Summary** — narrative paragraph. Expected content:

    > No gaps. All nine Phase 08 must-haves are satisfied by the actual codebase and planning artifacts:
    >
    > 1. **DiagramConfig waterfall (5 truths from 08-01):** --plantuml-path and --mermaid-path flags are wired through `Cli` → `CliOverrides` → `Config.diagram_config` → `MarkdownConverter::new()` for update and upload arms. The convert arm constructs DiagramConfig inline (documented deviation) and still respects both flags. Defaults ("plantuml", "mmdc") are preserved when no override is supplied. Three new unit tests (test_plantuml_path_cli_override, test_mermaid_path_cli_override, test_diagram_config_defaults_when_no_override) and one integration test (test_convert_with_diagram_path_flags) confirm end-to-end behavior.
    > 2. **Nyquist compliance (4 truths from 08-02):** Phase 01, 02, and 03 VALIDATION.md files now have `nyquist_compliant: true`, `wave_0_complete: true`, `status: verified`, and `audited: 2026-04-14` frontmatter — Phase 01 had no frontmatter before Phase 08 and received a fresh block; Phases 02 and 03 were flipped from `false` to `true`.
    >
    > One intentional deviation noted: the convert arm in src/lib.rs does not route through Config::load() (it has no Confluence credentials to validate); this is a documented SCAF-03 integration gap tracked for Phase 9 closure per REQUIREMENTS.md traceability table.

13. **Footer**:

    ```
    ---
    _Verified: 2026-04-20_
    _Verifier: Claude (gsd-verifier)_
    ```

**After writing the file:** run `markdownlint --fix .planning/phases/08-diagramconfig-waterfall-and-nyquist-compliance/08-VERIFICATION.md` to clean up formatting (this project uses markdownlint per CLAUDE.md).

**Evidence fidelity rule (CRITICAL, per 09-RESEARCH.md Pitfall 5):** Every Evidence cell MUST cite the **current tree's** file path + line number + quoted text excerpt. Do NOT copy-paste claims from 08-01-SUMMARY.md or 08-02-SUMMARY.md without independently verifying each claim with a fresh `grep` or file read. Task 1 gathered this evidence; Task 2 transcribes it into table cells.

**Line-number fidelity rule:** If Task 1 recorded a different line number than the examples above (e.g., comments were added, whitespace shifted), use the Task 1 number. The examples above reflect the file state at research time (2026-04-20) but may drift by ±1-2 lines.
  </action>
  <verify>
    <automated>test -f /Users/matthewstump/src/confluence-workflow/.planning/phases/08-diagramconfig-waterfall-and-nyquist-compliance/08-VERIFICATION.md && grep -q "^phase: 08-diagramconfig-waterfall-and-nyquist-compliance" /Users/matthewstump/src/confluence-workflow/.planning/phases/08-diagramconfig-waterfall-and-nyquist-compliance/08-VERIFICATION.md && grep -q "^status: passed" /Users/matthewstump/src/confluence-workflow/.planning/phases/08-diagramconfig-waterfall-and-nyquist-compliance/08-VERIFICATION.md && grep -q "^score: 9/9" /Users/matthewstump/src/confluence-workflow/.planning/phases/08-diagramconfig-waterfall-and-nyquist-compliance/08-VERIFICATION.md && grep -c "VERIFIED" /Users/matthewstump/src/confluence-workflow/.planning/phases/08-diagramconfig-waterfall-and-nyquist-compliance/08-VERIFICATION.md | awk '{exit ($1 < 9)}'</automated>
  </verify>
  <acceptance_criteria>
    - File exists at .planning/phases/08-diagramconfig-waterfall-and-nyquist-compliance/08-VERIFICATION.md
    - First line is `---` (opening YAML frontmatter delimiter)
    - Frontmatter contains `phase: 08-diagramconfig-waterfall-and-nyquist-compliance`
    - Frontmatter contains `verified: 2026-04-20T00:00:00Z`
    - Frontmatter contains `status: passed` (or `gaps_found` if Task 1 uncovered gaps)
    - Frontmatter contains `score: 9/9 must-haves verified` (or actual count)
    - Frontmatter contains `overrides_applied: 0` (or actual count)
    - File contains H1 heading `# Phase 8: DiagramConfig Waterfall and Nyquist Compliance Verification Report`
    - File contains `## Goal Achievement`
    - File contains `### Observable Truths`
    - File contains `### Deferred Items`
    - File contains `### Required Artifacts`
    - File contains `### Key Link Verification`
    - File contains `### Data-Flow Trace`
    - File contains `### Behavioral Spot-Checks`
    - File contains `### Requirements Coverage`
    - File contains `### Anti-Patterns Found`
    - File contains `### Human Verification Required`
    - File contains `### Gaps Summary`
    - File contains `SCAF-03` at least once (in Requirements Coverage)
    - File contains `VERIFIED` at least 9 times (one per Observable Truth row)
    - File contains `src/cli.rs` at least twice (once in artifacts, once in Evidence cells)
    - File contains `src/config.rs` at least three times (in artifacts, key links, truths)
    - File contains `src/lib.rs` at least three times
    - File contains `tests/cli_integration.rs` at least once (evidence for Truth #4)
    - File contains footer `_Verified: 2026-04-20_`
    - File contains footer `_Verifier: Claude (gsd-verifier)_`
    - `markdownlint .planning/phases/08-diagramconfig-waterfall-and-nyquist-compliance/08-VERIFICATION.md` produces zero errors (or only errors auto-fixed by `--fix`)
  </acceptance_criteria>
  <done>08-VERIFICATION.md exists with valid frontmatter, all 9 Phase-08 truths evidenced with file:line citations, Requirements Coverage shows SCAF-03 SATISFIED with traceability note to Phase 9 integration gap, no markdownlint errors remain.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Verifier claim → audit record | VERIFICATION.md content is consumed by downstream audit tooling and future milestone reviews; inaccurate file:line evidence undermines auditability |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering (of audit record) | 08-VERIFICATION.md evidence cells | mitigate | Task 1 mandates fresh grep/file-read for every claim BEFORE Task 2 writes the file. Acceptance criteria enforce that file contains specific path strings (src/cli.rs, src/config.rs, src/lib.rs, tests/cli_integration.rs) — a VERIFICATION.md that only cites SUMMARY narrative without file paths will fail verification. The plan runs in Wave 1 BEFORE 09-02's code refactor so evidence reflects Phase 08's state at close, not post-refactor state (09-RESEARCH.md Pitfall 5). |
| T-09-02 | Information Disclosure | VERIFICATION.md in git | accept | File is a planning artifact in the public repo; contains no secrets. `~/.claude/settings.json` is NOT referenced with contents — only the code path that reads it (`load_from_claude_config`). No disclosure surface. |
</threat_model>

<verification>
```bash
# Verify the artifact exists and has required structure
test -f .planning/phases/08-diagramconfig-waterfall-and-nyquist-compliance/08-VERIFICATION.md

# Verify frontmatter keys

grep -E "^(phase|verified|status|score|overrides_applied):" \
  .planning/phases/08-diagramconfig-waterfall-and-nyquist-compliance/08-VERIFICATION.md

# Verify all 9 truths were transcribed (count VERIFIED markers)

grep -c "VERIFIED" .planning/phases/08-diagramconfig-waterfall-and-nyquist-compliance/08-VERIFICATION.md

# Should be >= 9 (one per truth) + rows in Required Artifacts + rows in Key Link Verification

# Verify evidence cites real source files

grep -E "src/(cli|config|lib)\.rs" \
  .planning/phases/08-diagramconfig-waterfall-and-nyquist-compliance/08-VERIFICATION.md

# Verify Requirements Coverage row for SCAF-03

grep -A1 "SCAF-03" .planning/phases/08-diagramconfig-waterfall-and-nyquist-compliance/08-VERIFICATION.md

# Markdown lint passes

markdownlint .planning/phases/08-diagramconfig-waterfall-and-nyquist-compliance/08-VERIFICATION.md

```
</verification>

<success_criteria>
1. `.planning/phases/08-diagramconfig-waterfall-and-nyquist-compliance/08-VERIFICATION.md` exists with the full 13-section structure matching 07-VERIFICATION.md
2. All 9 Phase-08 must-have truths (5 from 08-01, 4 from 08-02) appear in the Observable Truths table with VERIFIED status and file:line evidence
3. SCAF-03 row in Requirements Coverage shows SATISFIED with specific line-number evidence + cross-reference to Phase 9 gap closure
4. Every Evidence cell cites a file path (e.g., `src/cli.rs`), a line number or range (e.g., `:33-35`), and a quoted code excerpt
5. `markdownlint` runs clean on the new file
6. Frontmatter status is `passed` (or `gaps_found` if Task 1 identified a regression, which is not expected based on 08-VALIDATION.md)
7. No override block is needed (overrides_applied: 0) because Phase 08 plans' truth wordings match the implementation
</success_criteria>

<output>
After completion, create `.planning/phases/09-convert-waterfall-fix-and-phase-08-verification/09-01-SUMMARY.md`
</output>
