---
phase: 08-diagramconfig-waterfall-and-nyquist-compliance
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-project-scaffolding-and-confluence-api-client/01-VALIDATION.md
  - .planning/phases/02-markdown-to-confluence-storage-format-converter/02-VALIDATION.md
  - .planning/phases/03-llm-client-and-comment-preserving-merge/03-VALIDATION.md
autonomous: true
requirements:
  - SCAF-03

must_haves:
  truths:
    - "Phase 01 VALIDATION.md has YAML frontmatter with nyquist_compliant: true and wave_0_complete: true"
    - "Phase 02 VALIDATION.md has nyquist_compliant: true and wave_0_complete: true in frontmatter"
    - "Phase 03 VALIDATION.md has nyquist_compliant: true and wave_0_complete: true in frontmatter"
    - "All three VALIDATION.md files have an audited: date field"
  artifacts:
    - path: ".planning/phases/01-project-scaffolding-and-confluence-api-client/01-VALIDATION.md"
      provides: "Nyquist-compliant validation strategy for Phase 01"
      contains: "nyquist_compliant: true"
    - path: ".planning/phases/02-markdown-to-confluence-storage-format-converter/02-VALIDATION.md"
      provides: "Nyquist-compliant validation strategy for Phase 02"
      contains: "nyquist_compliant: true"
    - path: ".planning/phases/03-llm-client-and-comment-preserving-merge/03-VALIDATION.md"
      provides: "Nyquist-compliant validation strategy for Phase 03"
      contains: "nyquist_compliant: true"
  key_links: []
---

<objective>
Achieve Nyquist compliance for Phases 01, 02, and 03 by updating their VALIDATION.md files with compliant frontmatter. Phase 01 needs a frontmatter block added from scratch; Phases 02 and 03 need existing frontmatter values changed from false to true.

Purpose: The gsd validation tooling requires `nyquist_compliant: true` and `wave_0_complete: true` in VALIDATION.md frontmatter. Phases 01-03 were created before this convention was established. This plan closes the compliance gap.

Output: Three updated VALIDATION.md files with compliant frontmatter.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-credential-waterfall-fix/06-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify Wave 0 items exist and tests pass for Phases 01-03</name>
  <files></files>
  <read_first>
    - .planning/phases/01-project-scaffolding-and-confluence-api-client/01-VALIDATION.md (Wave 0 items and test commands)
    - .planning/phases/02-markdown-to-confluence-storage-format-converter/02-VALIDATION.md (Wave 0 items listed at lines 54-57)
    - .planning/phases/03-llm-client-and-comment-preserving-merge/03-VALIDATION.md (Wave 0 items listed at lines 58-64)
  </read_first>
  <action>
Before setting `wave_0_complete: true`, verify the Wave 0 items actually exist and their tests pass.

**Phase 01 verification:** Run these commands and confirm they pass:

```bash
cargo build 2>&1 | tail -5          # SCAF-01
cargo test --lib config::tests      # SCAF-03, SCAF-04
cargo test --lib error               # SCAF-05
cargo test confluence                 # CONF-01 through CONF-05
```

**Phase 02 verification:** Check that the Wave 0 files exist and tests pass:

```bash
# Check file existence
ls src/converter/tests.rs 2>/dev/null || echo "MISSING: src/converter/tests.rs"
ls tests/fixtures/ 2>/dev/null || echo "MISSING: tests/fixtures/"

# Run converter tests
cargo test converter
```

**Phase 03 verification:** Check that the Wave 0 files exist and tests pass:

```bash
# Check file existence
ls src/llm/mod.rs src/merge/mod.rs src/merge/extractor.rs src/merge/matcher.rs src/merge/injector.rs 2>/dev/null

# Run merge and llm tests
cargo test --lib merge
cargo test --lib llm
```

If any Wave 0 item is genuinely missing (file does not exist, test does not compile), do NOT set `wave_0_complete: true` for that phase. Instead, document which items are missing in the SUMMARY. If files exist and tests pass (or tests exist as stubs that pass), proceed.
  </action>
  <verify>
    <automated>cd /Users/matthewstump/src/confluence-workflow && cargo test --lib config::tests && cargo test converter && cargo test --lib merge && cargo test --lib llm</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --lib config::tests` exits 0
    - `cargo test converter` exits 0
    - `cargo test --lib merge` exits 0
    - `cargo test --lib llm` exits 0
    - src/converter/tests.rs exists (verified by ls)
    - src/llm/mod.rs exists (verified by ls)
    - src/merge/extractor.rs exists (verified by ls)
    - src/merge/matcher.rs exists (verified by ls)
    - src/merge/injector.rs exists (verified by ls)
  </acceptance_criteria>
  <done>All Wave 0 files confirmed to exist and all relevant test suites pass, validating that wave_0_complete: true is accurate for all three phases.</done>
</task>

<task type="auto">
  <name>Task 2: Update VALIDATION.md frontmatter for Phases 01, 02, and 03</name>
  <files>
    .planning/phases/01-project-scaffolding-and-confluence-api-client/01-VALIDATION.md,
    .planning/phases/02-markdown-to-confluence-storage-format-converter/02-VALIDATION.md,
    .planning/phases/03-llm-client-and-comment-preserving-merge/03-VALIDATION.md
  </files>
  <read_first>
    - .planning/phases/01-project-scaffolding-and-confluence-api-client/01-VALIDATION.md (currently has NO frontmatter block)
    - .planning/phases/02-markdown-to-confluence-storage-format-converter/02-VALIDATION.md (frontmatter lines 1-8, nyquist_compliant: false, wave_0_complete: false)
    - .planning/phases/03-llm-client-and-comment-preserving-merge/03-VALIDATION.md (frontmatter lines 1-8, nyquist_compliant: false, wave_0_complete: false)
    - .planning/phases/06-credential-waterfall-fix/06-VALIDATION.md (reference implementation of compliant frontmatter)
  </read_first>
  <action>
**Phase 01 VALIDATION.md** -- Prepend a YAML frontmatter block as the very first lines of the file (before the existing `# Phase 01 Validation` heading). The file currently has NO `---` delimiters:

```yaml
---
phase: 1
slug: project-scaffolding-and-confluence-api-client
status: verified
nyquist_compliant: true
wave_0_complete: true
created: 2026-04-10
audited: 2026-04-14
---
```

Leave the rest of the file content unchanged. After prepending, the file should start with `---` on line 1 and the closing `---` before the `# Phase 01 Validation` heading.

**Phase 02 VALIDATION.md** -- Edit the existing frontmatter (lines 1-8). Change these three values and add `audited`:

- Line 4: `status: draft` -> `status: verified`
- Line 5: `nyquist_compliant: false` -> `nyquist_compliant: true`
- Line 6: `wave_0_complete: false` -> `wave_0_complete: true`
- Add after line 7: `audited: 2026-04-14`

Result:

```yaml
---
phase: 2
slug: markdown-to-confluence-storage-format-converter
status: verified
nyquist_compliant: true
wave_0_complete: true
created: 2026-04-10
audited: 2026-04-14
---
```

**Phase 03 VALIDATION.md** -- Edit the existing frontmatter (lines 1-8). Same changes as Phase 02:

- Line 4: `status: draft` -> `status: verified`
- Line 5: `nyquist_compliant: false` -> `nyquist_compliant: true`
- Line 6: `wave_0_complete: false` -> `wave_0_complete: true`
- Add after line 7: `audited: 2026-04-14`

Result:

```yaml
---
phase: 3
slug: llm-client-and-comment-preserving-merge
status: verified
nyquist_compliant: true
wave_0_complete: true
created: 2026-04-10
audited: 2026-04-14
---
```

After all three edits, run `markdownlint --fix .planning/phases/01-project-scaffolding-and-confluence-api-client/01-VALIDATION.md .planning/phases/02-markdown-to-confluence-storage-format-converter/02-VALIDATION.md .planning/phases/03-llm-client-and-comment-preserving-merge/03-VALIDATION.md` to ensure markdown formatting is clean.
  </action>
  <verify>
    <automated>cd /Users/matthewstump/src/confluence-workflow && grep -c "nyquist_compliant: true" .planning/phases/01-project-scaffolding-and-confluence-api-client/01-VALIDATION.md .planning/phases/02-markdown-to-confluence-storage-format-converter/02-VALIDATION.md .planning/phases/03-llm-client-and-comment-preserving-merge/03-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - 01-VALIDATION.md starts with `---` on line 1 (frontmatter block exists)
    - 01-VALIDATION.md contains `nyquist_compliant: true`
    - 01-VALIDATION.md contains `wave_0_complete: true`
    - 01-VALIDATION.md contains `audited: 2026-04-14`
    - 02-VALIDATION.md contains `nyquist_compliant: true` (was `false`)
    - 02-VALIDATION.md contains `wave_0_complete: true` (was `false`)
    - 02-VALIDATION.md contains `status: verified` (was `draft`)
    - 02-VALIDATION.md contains `audited: 2026-04-14`
    - 03-VALIDATION.md contains `nyquist_compliant: true` (was `false`)
    - 03-VALIDATION.md contains `wave_0_complete: true` (was `false`)
    - 03-VALIDATION.md contains `status: verified` (was `draft`)
    - 03-VALIDATION.md contains `audited: 2026-04-14`
    - grep "nyquist_compliant: true" returns 1 match per file (3 total)
  </acceptance_criteria>
  <done>All three VALIDATION.md files have compliant frontmatter with nyquist_compliant: true, wave_0_complete: true, status: verified, and audited date.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

No trust boundaries are relevant to this plan -- it modifies only planning artifacts (VALIDATION.md files), not code.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| (none) | — | — | — | No code changes in this plan; VALIDATION.md files are planning artifacts with no runtime impact. No threats applicable. |
</threat_model>

<verification>
```bash
# Verify all three files have compliant frontmatter
grep "nyquist_compliant: true" .planning/phases/01-*/01-VALIDATION.md
grep "nyquist_compliant: true" .planning/phases/02-*/02-VALIDATION.md
grep "nyquist_compliant: true" .planning/phases/03-*/03-VALIDATION.md

grep "wave_0_complete: true" .planning/phases/01-*/01-VALIDATION.md
grep "wave_0_complete: true" .planning/phases/02-*/02-VALIDATION.md
grep "wave_0_complete: true" .planning/phases/03-*/03-VALIDATION.md

# Verify Phase 01 has frontmatter (starts with ---)

head -1 .planning/phases/01-*/01-VALIDATION.md  # should be "---"

```
</verification>

<success_criteria>
1. All three VALIDATION.md files contain `nyquist_compliant: true` in YAML frontmatter
2. All three VALIDATION.md files contain `wave_0_complete: true` in YAML frontmatter
3. All three VALIDATION.md files contain `audited: 2026-04-14`
4. Phase 01 VALIDATION.md has a proper `---` delimited frontmatter block (did not have one before)
5. Phases 02 and 03 VALIDATION.md have `status: verified` (was `draft`)
</success_criteria>

<output>
After completion, create `.planning/phases/08-diagramconfig-waterfall-and-nyquist-compliance/08-02-SUMMARY.md`
</output>
